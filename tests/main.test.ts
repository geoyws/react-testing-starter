import { it, expect, describe } from "vitest";

describe("group", () => {
  it("test", () => {
    expect(1).toBe(1);
  });

  it("test2", () => {
    expect(2).toBe(2);
  });

  it("test3", () => {
    expect(3).toBe(3);
  });
});
